name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - '*'

jobs:
  build:
    
    runs-on: macOS-latest

    steps:
    - name: Tests
    - run: make
    - run: bash <(curl -s https://codecov.io/bash)
